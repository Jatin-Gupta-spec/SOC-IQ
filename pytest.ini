[pytest]
markers =
    real_settings_base_dir: this test intentionally exercises the real, unpatched production default settings path (BASE_DIR/config/settings.json) and must not be isolated by tests/conftest.py's autouse D2 fixture.
